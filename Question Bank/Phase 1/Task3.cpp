#include <iostream>

using namespace std;

int main() {
    // Declare variables
    double x;

    // Solve the equation: 2(x - 3) = 4x - 1
    // Expand and simplify the equation
    // 2x - 6 = 4x - 1

    // Move the x terms to one side of the equation and the constants to the other side
    // 2x - 4x = -1 + 6
    // -2x = 5

    // Divide by -2 to solve for x
    // x = 5 / -2

    x = 5.0 / -2.0;

    // Display the result
    cout << "The value of x is: " << x << endl;

    return 0;
}

